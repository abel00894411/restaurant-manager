package com.restaurante.proyecto.controllers;

import org.springframework.web.bind.annotation.RestController;

@RestController("/menu") // aqui si todas los controladores de esta clase ya por default tienen la ruta '/menu'
// esto no se hizo en UsuarioController porque no todos son '/usuarios' hay algunos que son '/usuario'
public class MenuController {
	
	
	
	
}
